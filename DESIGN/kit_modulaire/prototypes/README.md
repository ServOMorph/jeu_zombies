# Prototypes de prévisualisation

Ces scènes constituent les sources DESIGN du kit V1. Elles sont volontairement sans collision, script, lumière ni animation. Les copies de validation sont placées dans `DESIGN/laboratoire/imports/`.
